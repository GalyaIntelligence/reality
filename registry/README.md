# Galya Reality Registry

Human-browsable catalog generated from [`index.json`](./index.json).

Language support is per-validator: Python only, TypeScript only, or both.
At least one language is required. The `status` field records completeness.

| Name | Status | Latency | Maintainer | Repo |
|------|--------|---------|------------|------|
| *(empty — add the first entry via PR)* | — | — | — | — |

To add a validator, open a PR that adds exactly one entry to `index.json`.
See [CONTRIBUTING.md](../CONTRIBUTING.md).
